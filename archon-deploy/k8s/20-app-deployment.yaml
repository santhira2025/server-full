apiVersion: apps/v1
kind: Deployment
metadata:
  name: archon-app
  namespace: archon
  labels:
    app: archon-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: archon-app
  template:
    metadata:
      labels:
        app: archon-app
    spec:
      containers:
        - name: archon-app
          image: archon-app:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
              name: http
          resources:
            requests:
              memory: "64Mi"
              cpu: "100m"
            limits:
              memory: "128Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: archon-app
  namespace: archon
spec:
  selector:
    app: archon-app
  ports:
    - port: 80
      targetPort: 80
      name: http
  type: ClusterIP
